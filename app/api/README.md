# Future API routes

Keep provider credentials and persistence logic in server-only route handlers here. Suggested endpoints include `/api/search`, `/api/interrogate`, `/api/agora`, `/api/forum`, `/api/extract`, and `/api/import/preview`.
