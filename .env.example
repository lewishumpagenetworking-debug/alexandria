# Add only server-side secrets here. Never commit a populated .env.local file.
OPENAI_API_KEY=
OPENAI_MODEL=

# Future database provider. Not required for the prototype.
DATABASE_URL=

# Optional future MCP server URL.
MCP_SERVER_URL=

# Public URL only; this endpoint holds OPENAI_API_KEY server-side.
NEXT_PUBLIC_ALEXANDRIA_AI_ENDPOINT=
